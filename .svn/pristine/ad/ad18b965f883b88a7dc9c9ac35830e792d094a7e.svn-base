/**
 * 
 */
package com.aurionpro.generic.fieldvalidator;

import java.util.LinkedList;
import java.util.List;

import com.aurionpro.generic.factoryvalidator.AbstractFieldValidator;



// TODO: Auto-generated Javadoc
/**
 * The Class QuestionAnswerValidator.
 *
 * @author bhanu.shukla
 */
public class QuestionAnswerValidator extends AbstractFieldValidator {

	/** The min question count. */
	private final int MIN_QUESTION_COUNT = 2;

	/*
	 * (non-Javadoc)
	 * 
	 * @see
	 * com.aurionpro.cashpro.rest.validator.factoryvalidator.AbstractFieldValidator
	 * #validate(java.lang.Object)
	 */
	
	
	/**
	 * TODO:Pravin to check this
	 */
	
	@SuppressWarnings("unchecked")
	@Override
	public List<String> validate(Object obj) {

		List<String> errorList = new LinkedList<String>();
		/*List<SecurityQuestionAnswers> questionAnswers = (List<SecurityQuestionAnswers>) obj;
		if (questionAnswers.size() >= MIN_QUESTION_COUNT) {
			for (SecurityQuestionAnswers seQuestionAnswers : questionAnswers) {
				if (RestWebUtils.isEmpty(seQuestionAnswers.getQuestionId())
						|| RestWebUtils.isEmpty(seQuestionAnswers.getAnswer())) {
					errorList
							.add("MSGKEY_VALIDATOR_QUESTION_ANSWER_VALIDATION");
				}
			}
		}
		if (nextFieldValidator != null && errorList.isEmpty()) {
			errorList.addAll(nextFieldValidator.validate(obj));
		}*/
		return errorList;
	}

}
